#ifndef CONFIGURATION_SETTINGS_H
#define CONFIGURATION_SETTINGS_H

// Provides access to the configuration settings.
class ConfigurationSettings {
    // Saves the changes to the FRAM module.
    void saveChanges();
};

#endif